
> **Note:** If a notebook (`.ipynb`) won't preview on GitHub and shows an
> "An error occurred" page, this is a GitHub rendering issue, not a problem with the
> file. Just download the notebook (click **Raw** or the download icon) and open it in
> Jupyter or VS Code — all outputs are saved in the file.
